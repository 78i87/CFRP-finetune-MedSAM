regime: zero_shot
model:
  backend: auto
  sam2_config: configs/sam2.1_hiera_t512.yaml
  checkpoint: checkpoints/MedSAM2_latest.pt
  image_size: 512
train_volumes: []
val_volumes:
  - data/processed/cfrp/val_00.npz
image_size: 512
batch_size: 2
epochs: 0
ckpt_dir: checkpoints
log_path: logs/zero_shot.csv
prompt_mode: bbox
